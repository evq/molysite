{
    "output": [
        {
            "many": "${replace(var.sub_domain, \".\", \"\\\\\\\\\\\\\\\\.\")}",
            "one": "${replace(var.sub_domain, \".\", \"\\\\.\")}",
            "two": "${replace(var.sub_domain, \".\", \"\\\\\\\\.\")}"
        }
    ]
}
